name: Deploy and Verify

on:
  push:
    branches:
      - main

jobs:
  deploy: verify-meta-tag
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # Your deployment steps go here
      - name: Deploy site
        run: |
          echo "🚀 Deploying site..."
          # add your actual deploy commands

  verify-meta-tag:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - name: Check homepage for Base meta tag
        run: |
          RESPONSE=$(<meta name="base:app_id" content="69dd7b512755eb4e67523000" />
          if echo "$RESPONSE" | grep -q '<meta name="base:app_id" content="69dd7b512755eb4e67523000" />'; then
            echo "✅ Meta tag detected!"
          else
            echo "❌ Meta tag not found."
            exit 1
          fi
