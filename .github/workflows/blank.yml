name: Deploy and Verify

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy site
        run: |
          echo "🚀 Deploying site..."
          # add your actual deploy commands here

  verify-meta-tag:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - name: Check homepage for Base meta tag
        run: |
          RESPONSE=$(curl -s https://your-site-url.com)
          if echo "$RESPONSE" | grep -q '<meta name="base:app_id" content="69dd7b512755eb4e67523000" />'; then
            echo "✅ Meta tag detected!"
          else
            echo "❌ Meta tag not found."
            exit 1
          fi
